# Dependencies
node_modules/

# App build outputs (not the GitHub Pages site)
dist/
dist-internal/
dist-en/
dist-tr/

# Legacy English build source (large binary files, kept locally only)
distribution/github-pages/legacy-en-build/

# Desktop distribution builds (not web-related)
distribution/miniapps-en-mac/

# Large local runtime binaries
local-runtime/bin/

# Python virtual environments
**/.venv/

# Stem helper build outputs (binaries, zip artifacts — distributed via GitHub Releases)
distribution/stem-helper-mac/
distribution/stem-helper/*.zip
distribution/github-pages/site/downloads/

# OS
.DS_Store

# Editor
.vscode/
.idea/

# Local env
*.local
.env
.env.*
!.env.example
